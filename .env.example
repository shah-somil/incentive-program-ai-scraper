# Required: the pipeline LLM-parses every page, so an OpenAI key is needed.
OPENAI_API_KEY=

# Optional - override the OpenAI model. Default: gpt-4o-mini.
# For higher-fidelity extraction try gpt-4o or claude-via-openai-compatible.
OPENAI_MODEL=gpt-4o-mini
